package com.googleplaybot.events.remote;

public class ResponseEvent {

    public boolean isSuccessful;

    public ResponseEvent(boolean isSuccessful) {
        this.isSuccessful = isSuccessful;
    }
}
